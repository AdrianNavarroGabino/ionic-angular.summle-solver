import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Summle solver',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
